
const helpers = require('../../helpers')

// TODO
module.exports = {
  command: 'create <folder-id>',
  desc: 'Create a folder.',
  builder: {},
  handler: async function (argv) {
    console.log('ERROR: createFolder() not yet implemented in node-ghost-inspector')
    process.exit(1)
  }
}